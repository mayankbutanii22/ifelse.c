#include<stdio.h>
main()
{
	int a,b,c;
	
	printf("Enter a value of three number: ");
	scanf("%d%d%d",&a,&b,&c);
    if  (max=a;)
    if (max < b)
        max = b;
    if (max < c)
        max = c;
 
    printf("The largest number %d, %d, %d is: %d", a, b, c,max);
}